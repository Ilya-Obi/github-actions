name: Demo Workflow
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print to console
        run: -e "echo Hello GH Actions!"
      - name: Print new lines
        run:
          echo -e $"$0"
          echo -e "Line 2"
          echo -e "Hello world"
